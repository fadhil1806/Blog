/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'stablo.web3templates.com'], // Add your external image domains here
      },
};

export default nextConfig;
